# Security

The backup file may contain bookmark URLs and other nightTab configuration. Review the JSON before replacing it with a personal export. Never publish passwords, API keys, access tokens, or other secrets.
