# Repository Plan

The repository distributes a portable nightTab backup and documents the exact restore workflow.

Release assets are created automatically when a `v*` tag is pushed. The release asset is always named `nighttab-backup.json`, allowing the stable latest-release URL used by the README.
